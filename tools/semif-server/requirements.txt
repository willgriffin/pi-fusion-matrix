# Pinned to the versions upstream SemIf measures against (its pyproject pins the same set), because the
# readout is a single forward pass and kernel/dtype differences move borderline probabilities.
# The CUDA build of torch is required: upstream's loader demands exactly one visible CUDA device.
torch==2.10.0
transformers==5.17.0
accelerate==1.12.0
safetensors==0.8.0
huggingface-hub==1.31.0
tokenizers==0.23.2
numpy==2.2.6
sentencepiece==0.2.1
protobuf==7.36.1